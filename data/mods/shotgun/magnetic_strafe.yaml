id: magnetic_strafe
name: Magnetic Strafe
rarity: rare
polarity: madurai
base_drain: 4                 # max drain 7 at rank 3
max_rank: 3
# The wiki module gives this one a `/Lotus/Language/…` key — a localisation
# entry, not the item — so it joined to NOTHING in the WFCD export and its
# image went missing (2026-08-03). The item's real `uniqueName` is below. It is
# the only mod in `data/` this happened to, and its drain/rank/polarity/rarity
# happen to match anyway; the join being broken is the bug, not the numbers.
internal_name: /Lotus/Upgrades/Mods/Shotgun/DualStat/MagneticFireRateShotgunMod
description: "+X% Magnetic\n+X% Fire Rate"

effects:
  - kind: elemental_damage_bonus
    element: magnetic
    rank0: 0.15
    rankMax: 0.6
  - kind: fire_rate_bonus
    rank0: 0.1
    rankMax: 0.4

source:
  url: https://wiki.warframe.com/w/Magnetic_Strafe
