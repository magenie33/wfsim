id: apex_predator
name: Apex Predator
rarity: uncommon
polarity: naramon
base_drain: 2                 # max drain 7 at rank 5
max_rank: 5
exilus: true
internal_name: /Lotus/Upgrades/Mods/PvPMods/Rifle/MarkTargetRifleMod
description: "On Hit:\nReveals target on Minimap for +Xs."

effects:
  # NOT LOADED, both entries: `mods_data::effect()` has no `unmodeled` arm, so
  # they are dropped and the mod loads with NO effects (data/README.md). Target
  # marking is UI, not damage.
  # "On Hit:"
  - kind: unmodeled
  # "Reveals target on Minimap for +6s."
  - kind: unmodeled
    rank0: 0.01
    rankMax: 0.06

source:
  url: https://wiki.warframe.com/w/Apex_Predator
