id: critical_delay
name: Critical Delay
rarity: rare
polarity: naramon
base_drain: 4                 # max drain 9 at rank 5
max_rank: 5
internal_name: /Lotus/Upgrades/Mods/Rifle/DualStat/CorruptedCritRateFireRateRifle
description: "+X% Critical Chance\n-X% Fire Rate (x2 for Bows)"

# The "(x2 for Bows)" clause is a LITERAL - the multiplier does not scale
# with rank (WFCD levelStats). Bows double this mod's fire-rate effect;
# no bow is in data/weapons yet, so nothing models it.
effects:
  - kind: crit_chance_bonus
    rank0: 0.3333
    rankMax: 2.0
  # DRAWBACK: Critical Delay COSTS fire rate (-20% at max), it does not
  # grant it. This was the placeholder pair (rank0 1/6, rankMax 1.0), so the
  # sim read a penalty as a +100% bonus. WFCD ramp: -3.3% -> -20%.
  - kind: fire_rate_bonus
    rank0: -0.0333
    rankMax: -0.2

family: point_strike
incompatible_with: [point_strike]

source:
  url: https://wiki.warframe.com/w/Critical_Delay
