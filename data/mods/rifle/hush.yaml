id: hush
name: Hush
rarity: common
polarity: naramon
base_drain: 2                 # max drain 5 at rank 3
max_rank: 3
exilus: true
internal_name: /Lotus/Upgrades/Mods/Rifle/WeaponNoiseReductionMod
description: "Reduces the chance an enemy will hear gunfire by X%."

effects:
  # NOT LOADED (data/README.md "an effect the loader does not parse"):
  # `mods_data::effect()` has no `unmodeled` arm, so this hits `_ => return
  # None` and the mod loads with NO effects. Kept as the explicit record that
  # the payload is out of the sim's world — stealth, not damage.
  # "Reduces the chance an enemy will hear gunfire by 100%."
  - kind: unmodeled
    rank0: 0.25
    rankMax: 1.0

source:
  url: https://wiki.warframe.com/w/Hush
