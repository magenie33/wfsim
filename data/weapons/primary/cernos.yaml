id: cernos
name: Cernos
slot: primary
# A SILENT IMPACT BOW. The arsenal shows the CHARGED shot — the module's
# `_TooltipAttackDisplay` is 2 — which is 380 damage with a metre of punch
# through after half a second's draw; the tapped shot is half of it and carries
# none.
class: bow
form: charged
default_form: true          # the arsenal's form (module _TooltipAttackDisplay)
transform_group: cernos
mod_pools: [primary, rifle, bow]   # CompatibilityTags PROJECTILE
mastery_rank: 6
max_rank: 30

# Weapon-level metadata (wiki Module:Weapons/data/primary, cross-checked
# against WFCD warframe-items — 0 disagreements).
accuracy: 16.7
disposition: 1.3            # riven disposition
polarities: [madurai]
exilus_polarity: naramon
riven_family: Cernos
traits: [tenno]
introduced: "11.1"
internal_name: /Lotus/Weapons/Tenno/Bows/AntlerBow/AntlerBow
noise: silent
ammo_type: primary
ammo_max: 72
ammo_pickup: 15
magazine: 1
reload_seconds: 0.6

# Condition Overload: THE CATALOG NAMES THIS ATTACK. Verbatim
# (docs/CATALOGS.md §1):
#
#   Cernos | Charged Attack | Projectile | 380 | 190 | 50% | Adding
#
# Notes cell:
#   CO-bonus only applies to base (uncharged) damage; bows have innate 2x
#   damage multiplier when fully charged
#
# THIS ENTRY READ ADDING AT 100% UNTIL 2026-08-20, which was the
# ordinary class assumed because nobody had opened the catalog for this
# weapon — the check was run against our own transcription of it, which
# only ever carried the rows the roster already had.
co_behavior: additive_with_base_damage
co_base_fraction: 0.5

attack:
  # Cone half-angle from the reticle, degrees — wiki
  # Module:Weapons/data (MinSpread/MaxSpread on this attack). An ATTACK is
  # never inherited, so every form states its own.
  spread:
    min_deg: 0.0
    max_deg: 12.0
  trigger: charge
  shot_type: projectile
  projectile_speed_mps: 90
  fire_rate: 1
  charge_seconds: 0.5
  multishot: 1.0
  ammo_cost: 1
  crit_chance: 0.36
  crit_multiplier: 2.0
  status_chance: 0.18
  punch_through_m: 1.0
  # NO RANGE ROW on this attack — the page states none. See
  # data/surveys/weapon_range.yaml.
  damage: { impact: 342, puncture: 19, slash: 19 }   # total 380

unmodeled:
  - reason: silent_weapon

source:
  url: https://wiki.warframe.com/w/Cernos
