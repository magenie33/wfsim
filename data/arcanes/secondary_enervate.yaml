id: secondary_enervate
name: Secondary Enervate
kind: arcane
weapon_slot: secondary
rarity: rare
max_rank: 5
arcanes_to_max: 21
drop_chance: 0.125
perk: secondary_enervate   # the mechanic lives in the perk + the buff it grants

# Per-rank strength (wiki, https://wiki.warframe.com/w/Secondary_Enervate):
# +10% flat crit chance per stack at EVERY rank; only the reset threshold grows
# (big crits before the stack resets = rank+1, so rank 5 = 6). Mirrors
# engine::perks::secondary_enervate (from_rank).
ranks:
  - {flat_crit_per_stack: 0.10, reset_after_big_crits: 1}
  - {flat_crit_per_stack: 0.10, reset_after_big_crits: 2}
  - {flat_crit_per_stack: 0.10, reset_after_big_crits: 3}
  - {flat_crit_per_stack: 0.10, reset_after_big_crits: 4}
  - {flat_crit_per_stack: 0.10, reset_after_big_crits: 5}
  - {flat_crit_per_stack: 0.10, reset_after_big_crits: 6}

source:
  url: https://wiki.warframe.com/w/Secondary_Enervate

# Arcane item metadata only. Mechanic: data/perks/ + data/buffs/ secondary_enervate.
