# THE OFFICIAL BOARD for `single_target_v1` — GENERATED, never hand-edited.
#
# Rows are BUILDS that players submitted, each scored by running this engine
# over it under that benchmark with the benchmark's pinned seed. Nobody ever
# submits a number: a score here was computed here, so anyone with the repo can
# reproduce any row exactly — in a browser as well as natively (measured
# 2026-08-04: wasm and native agree to the last digit).
#
# EMPTY IS THE HONEST STATE until submissions arrive. It was briefly seeded with
# optimizer-found builds so the feature could be seen working; those are gone
# (user, 2026-08-04) because a board of our own builds presented as a board is
# the one thing it must not be.
#
# `source` says where the rows came from, and the UI repeats it: `submissions`
# once real ones land.
benchmark: single_target_v1
source: submissions
entries: []
