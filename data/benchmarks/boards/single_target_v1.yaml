# THE OFFICIAL BOARD for `single_target_v1` — GENERATED by `wfsim-board`.
#
# Rows are BUILDS players submitted; every score was computed here, by this
# engine, under that benchmark's own pinned seed. Nobody submits a number, so
# any row can be reproduced exactly by anyone with the repo.
#
# CLEARED 2026-08-04. The rows here were scored from records the endpoint had
# SORTED on the way in, and mods combine elements in the order they are listed —
# so the order those players built was destroyed before anything was measured,
# and re-scoring could only have produced a build that was legal and not theirs.
# The KV records were cleared with it; anything else would have republished them
# on the next run.
benchmark: single_target_v1
source: submissions
entries: []
