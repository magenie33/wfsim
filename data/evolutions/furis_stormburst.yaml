id: furis_stormburst
name: Stormburst
weapon: furis
tier: 2
icon: DamageAndOnHittingEnemyAffectedByElectricity(xWhite).png
description: "Increase Base Damage by +28.\nOn hitting an enemy affected by Electricity: +0.4 Multishot for 2s. Stacks up to 3x."
# THE GENESIS IS THE FAMILY'S. One Furis Incarnon Genesis upgrades either the
# Furis or the MK1-Furis, so both weapons carry the same ladder. Where a value
# DIFFERS between them the wiki prints it as a pair, and the number below is
# this weapon's.
selection: choose_one

# CONDITION OVERLOAD — this perk's added base is EXCLUDED from the CO term.
# The catalog's Furis row, verbatim:
#
#   Furis | Incarnon Mode | Projectile | 100 or 128 (with Evolution II) | 100 |
#     100% or 78% | Adding | "CO-bonus does not use base damage increase
#     Evolution"
#
# The arithmetic ties it down: the Incarnon form is 100 Heat, Evolution II adds
# 28, and 100/128 = 78% is the third column exactly. So CO keeps multiplying
# the UNEVOLVED 100 while the hit itself lands 128.
#
# On the TIER rather than on one option, unlike the Dual Toxocyst's row: that
# one names "Evolution II Perk 1" and its tier-mate is absent, so the flag had
# to be per perk. This row names "Evolution II" with no perk number, and BOTH
# of this tier's options grant the same +28 — there is no second reading.
co_base_excludes_this_evolution: true

effects:
  # THE SAME +28 its tier-mate gives, so this pair is decided entirely by the
  # second line — the same shape the Burston's tier 2 has.
  - kind: flat_base_damage
    value: 28.0
  # MODELLED (2026-08-07), as a LIVE stacking buff. It could not be a
  # `stacking_buff` with a multishot payload: that loads as
  # `AssumedMaxMultishot` — a total and a cap and NO trigger — so it would have
  # granted +1.2 multishot to a build carrying no Electricity at all. A live
  # buff is bumped inside the fight, where the target's debuffs are in hand, so
  # the condition it could not state statically is simply readable.
  #
  # A FLAT +0.4, not a percentage of base multishot — "+0.4 Multishot" — so it
  # joins `ms_eff` beside Final Fusillade's rather than the multishot bucket.
  #
  # ON THIS WEAPON IT ONLY PAYS AS A CYCLE, which the sim found rather than
  # anyone predicting it: the Incarnon form's base damage is Heat, so a
  # Convulsion's Electricity COMBINES with it and that form deals Radiation and
  # no Electricity whatever. The BASE form has no innate element, so its
  # Electricity stays raw — it is the half that puts the status on the target,
  # and the Incarnon half then finds it there. Measured: 657 pellets / 657
  # shots with no Electricity, 668 / 659 with Primed Convulsion.
  - kind: stacking_multishot_on_electricity_status
    per_stack: 0.4
    max_stacks: 3
    duration: 2

source:
  url: https://wiki.warframe.com/w/Furis_Incarnon_Genesis
