id: kunai_swift_conclusion
name: Swift Conclusion
weapon: kunai
tier: 2
description: "Increase Damage by +70. Increase Damage by +200% if enemy has less than half health."
# see notes: genesis_family_s_adapter
selection: choose_one

effects:
  # "Increase Damage by +70" — a BASE damage increase despite the card
  # dropping the word. The CO catalog's note for this weapon settles it:
  # "CO-bonus DOES use base damage increase Evolution", which is a sentence
  # about THIS perk (it is the only one on the ladder that adds damage) and
  # only means anything if the +70 is a base increase.
  #
  # It therefore feeds the CO base in full, which is this engine's default —
  # so no `co_base_excludes_this_evolution` here, deliberately.
  - kind: flat_base_damage
    value: 70.0
  # see notes: condition_target_evaluated_per
  #
  # see notes: bracket_stated_both_halves
  #
  # see notes: health_pools_front_target
  - kind: base_damage_below_half_health
    value: 2.0

source:
  url: https://wiki.warframe.com/w/Kunai_Incarnon_Genesis
