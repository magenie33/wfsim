id: mk1_furis_stormburst
name: Stormburst
weapon: mk1_furis
tier: 2
icon: DamageAndOnHittingEnemyAffectedByElectricity(xWhite).png
description: "Increase Base Damage by +34.\nOn hitting an enemy affected by Electricity: +0.4 Multishot for 2s. Stacks up to 3x."
# THE GENESIS IS THE FAMILY'S. One Furis Incarnon Genesis upgrades either the
# Furis or the MK1-Furis, so both weapons carry the same ladder. Where a value
# DIFFERS between them the wiki prints it as a pair, and the number below is
# this weapon's.
selection: choose_one

# CONDITION OVERLOAD — NOT excluded here, and that is a decision rather than an
# oversight. The catalog's row names "Furis" and nothing else; there is no
# MK1-Furis row, and its numbers are that weapon's (100 and 128 against this
# form's 60 and 94). The catalog lists discrepant ATTACKS one at a time, and
# absence from it is a positive statement that the attack behaves normally —
# Lato Vandal has a row and Lato Prime does not, same family and same Genesis.
# Copying the exclusion across on the strength of a shared adapter is exactly
# the generalisation that rule forbids.
#
# It is a genuine open question rather than a settled one: the two weapons take
# the same adapter, so DE plausibly applies the same code. If a measurement ever
# says so, this is the line to change — 60/94 = 64% would be the fraction.

effects:
  # THE SAME +34 its tier-mate gives, so this pair is decided entirely by the
  # second line — the same shape the Burston's tier 2 has.
  - kind: flat_base_damage
    value: 34.0
  # NOT LOADED, and this one is a JUDGEMENT rather than a gap. A `stacking_buff`
  # carrying a `multishot_bonus` loads as `AssumedMaxMultishot` — the total at
  # max stacks, permanently, with no trigger — which is right for Fevered
  # Frenzy (cast an ability and it is yours) and WRONG here: this one needs an
  # ELECTRICITY STATUS ON THE TARGET, so loading it that way would grant +1.2
  # multishot to a build carrying no Electricity at all. Left inert until a
  # stacking buff can state its condition. Worth up to +1.2 multishot to a
  # build that does run Electricity.
  #
  # NOT written as `stacking_buff`, and that is the whole point: that kind maps
  # a multishot payload to AssumedMaxMultishot NO MATTER what trigger is
  # written beside it, so the trigger would have been decoration and the +1.2
  # would have been granted. A kind the loader does not know is the only way to
  # say "modelled by nothing yet" and have it stay true.
  - kind: stacking_multishot_on_electricity_status
    per_stack: 0.4
    max_stacks: 3
    duration: 2

source:
  url: https://wiki.warframe.com/w/Furis_Incarnon_Genesis
