id: sybaris_prime_well_rehearsed
name: Well Rehearsed
weapon: sybaris_prime
tier: 2
description: "Increase Base Damage by +15. On Consecutive Weakpoint Hits: Increase Base Damage by +5. Stacks up to 3x."
# THE GENESIS IS THE FAMILY'S — one adapter upgrades every variant, so all
# of them carry the same ladder; a value that differs between them is
# printed per variant by the wiki and transcribed per variant here.
selection: choose_one

effects:
  # from: "Increase Base Damage by +15"
  - kind: flat_base_damage
    value: 15.0
  # NOT MODELLED — no rule for this clause, so it loads as
  # `EvoEffect::Inert` and both the builder and the optimizer print it
  # as such. VERBATIM: "On Consecutive Weakpoint Hits: Increase Base Damage by +5"
  - kind: unmodelled_on_consecutive_weakpoint_hits_increase_base_damage_by
  # NOT MODELLED — no rule for this clause, so it loads as
  # `EvoEffect::Inert` and both the builder and the optimizer print it
  # as such. VERBATIM: "Stacks up to 3x"
  - kind: unmodelled_stacks_up_to_x

source:
  url: https://wiki.warframe.com/w/Sybaris_Incarnon_Genesis
