id: synoid_gammacor_evo1_incarnon_form
name: Incarnon Form
weapon: synoid_gammacor
tier: 1
description: "Weakpoint hits charge Incarnon Transmutation; Alt Fire transmutes. Fires projectiles that pull enemies into a Cold Damage explosion."
# THE GENESIS IS THE FAMILY'S — one adapter upgrades every variant, so all
# of them carry the same ladder; a value that differs between them is
# printed per variant by the wiki and transcribed per variant here.
selection: fixed

effects:
  # LOADED as `EvoEffect::UnlocksForm(weapon)` — the form's stats are the
  # synoid_gammacor_incarnon entry's own, not perk payload.
  - kind: unlocks_weapon
    weapon: synoid_gammacor_incarnon

source:
  url: https://wiki.warframe.com/w/Gammacor_Incarnon_Genesis
