id: zylok_maulers_magazine
name: Mauler's Magazine
weapon: zylok
tier: 2
description: "Increase Base Damage by +76. On Reload From Empty: Increase Base Critical Damage Multiplier by +1x. Stacks up to 2x."
# THE GENESIS IS THE FAMILY'S — one adapter upgrades every variant, so all
# of them carry the same ladder; a value that differs between them is
# printed per variant by the wiki and transcribed per variant here.
selection: choose_one

effects:
  # from: "Increase Base Damage by +76"
  - kind: flat_base_damage
    value: 76.0
  # NOT MODELLED — no rule for this clause, so it loads as
  # `EvoEffect::Inert` and both the builder and the optimizer print it
  # as such. VERBATIM: "On Reload From Empty: Increase Base Critical Damage Multiplier by +1x"
  - kind: unmodelled_on_reload_from_empty_increase_base_critical_damage_mu
  # NOT MODELLED — no rule for this clause, so it loads as
  # `EvoEffect::Inert` and both the builder and the optimizer print it
  # as such. VERBATIM: "Stacks up to 2x"
  - kind: unmodelled_stacks_up_to_x

source:
  url: https://wiki.warframe.com/w/Zylok_Incarnon_Genesis
