id: gorgon_hoplite_virtue
name: Hoplite Virtue
weapon: gorgon
tier: 2
description: "Increase Base Damage by +10. On Shield Break: Increase Base Damage by +13 for 8 seconds."
# THE GENESIS IS THE FAMILY'S — one adapter upgrades every variant, so all
# of them carry the same ladder; a value that differs between them is
# printed per variant by the wiki and transcribed per variant here.
selection: choose_one

effects:
  # from: "Increase Base Damage by +10"
  - kind: flat_base_damage
    value: 10.0
  # NOT MODELLED — no rule for this clause, so it loads as
  # `EvoEffect::Inert` and both the builder and the optimizer print it
  # as such. VERBATIM: "On Shield Break: Increase Base Damage by +13 for 8 seconds"
  - kind: unmodelled_on_shield_break_increase_base_damage_by_for_seconds

source:
  url: https://wiki.warframe.com/w/Gorgon_Incarnon_Genesis
