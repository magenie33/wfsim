id: laetum_raptors_chase
name: Raptor's Chase
weapon: laetum
tier: 2
description: "+50% Movement Speed while Aiming."
selection: choose_one

effects:
  # NOT LOADED: `indirect` resolves to `EvoEffect::Inert` — the UI says "no
  # single-target DPS effect" and the engine takes nothing (data/README.md).
  # `stat` is unread; it is the column a movement model would consume.
  - kind: indirect          # mobility only
    stat: movement_speed_aiming
    value: 0.50

source:
  url: https://wiki.warframe.com/w/Laetum
