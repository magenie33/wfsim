// THE BOARD WAKES ON EVERY SOURCE AND SKIPS EVERY GENERATED TREE.
//
// Deciding when to re-score had two answers — a trigger list and a fingerprint
// — and they could disagree. A path in the fingerprint and not the trigger
// moves every stored score without waking anyone, so the full rescore it bought
// lands on whichever run happens next, for a reason that run has nothing to do
// with. `cli` was such a path: `cli/src/main.rs` is a demo that shoots a
// training dummy, and editing it invalidated the whole board in silence.
//
// AN EXCLUSION LIST INVERTS THAT FAILURE, which is the only reason to prefer
// it: forgetting an entry costs one wasted run, where forgetting an entry in an
// inclusion list costs a board that quietly stops moving. So this asserts the
// SHAPE — the board must name what to skip, never what to catch — and then that
// every skipped path is GENERATED and no source is skipped.
//
// GENERATED BY ANYONE, which is the property that matters and not "written by
// this workflow": nothing reads a generated tree to decide a number, so a push
// that only rewrites one cannot change what a score means. Two generators write
// one, and each list is read from the file that owns it rather than restated
// here — `GENERATED=` in the publish step, and `APP` in `build_site_app.py`.
//
// It is affordable because a run with nothing to do is one job: the
// `is there anything to score` step asks the scorer by walking the boards, and
// the fan-out does not start when the answer is none.
import { readFileSync } from "node:fs";
import { resolve, dirname } from "node:path";
import { fileURLToPath } from "node:url";

const ROOT = resolve(dirname(fileURLToPath(import.meta.url)), "..");
const NL = String.fromCharCode(10);
let bad = 0;
const check = (ok, name, detail) => {
  console.log(`  ${ok ? "ok  " : "FAIL"}  ${name}${ok || !detail ? "" : `  — ${detail}`}`);
  if (!ok) bad += 1;
};

const wf = readFileSync(resolve(ROOT, ".github/workflows/board.yml"), "utf8").split(NL);

// The list under a key, as workflow yaml writes one: `- "value"` lines until
// something that is not one.
const listAt = (key) => {
  const at = wf.findIndex((l) => l.trim() === key);
  if (at < 0) return null;
  const out = [];
  for (let i = at + 1; i < wf.length; i += 1) {
    const m = wf[i].match(/^\s+- "?([^"]+)"?\s*$/);
    if (!m) break;
    out.push(m[1]);
  }
  return out;
};

const skipped = listAt("paths-ignore:");
check(listAt("paths:") === null, "the board names what to SKIP, not what to catch",
  "an inclusion list cannot wake the board for a path nobody remembered to add");
check(Array.isArray(skipped) && skipped.length > 0,
  `the board names what it skips (${(skipped || []).join(" ") || "nothing"})`);

// WHAT THE RUN GENERATES, read from the step that commits it rather than
// restated — the two lists are the same claim about the same files.
const gen = wf.find((l) => l.trim().startsWith("GENERATED="));
const generated = gen
  ? gen.slice(gen.indexOf("=") + 1).replace(/"/g, "").trim().split(/\s+/).filter(Boolean)
  : [];
check(generated.length > 0, `publish names what it writes (${generated.join(" ") || "none"})`);

// THE OTHER GENERATOR, read from the constant that decides it. `site/` is the
// static deployment `build_site_app.py` writes; a path under it is output the
// same way `boards/` is, and the board is as unable to read one as the other.
const app = readFileSync(resolve(ROOT, "scripts/build_site_app.py"), "utf8")
  .match(/^APP = ROOT \/ "([^"]+)"/m);
check(!!app, `build_site_app names its output tree (${app ? app[1] + "/" : "APP not found"})`);
const trees = [...generated, ...(app ? [`${app[1]}/**`] : [])];

// A generated path covers a skipped one when it IS that path or a parent of it.
const covers = (g, p) => {
  const a = g.replace(/\/\*\*$/, "").replace(/\/$/, "");
  const b = p.replace(/\/\*\*$/, "").replace(/\/$/, "");
  return a === b || b.startsWith(`${a}/`);
};
for (const p of skipped || []) {
  check(
    trees.some((g) => covers(g, p)),
    `\`${p}\` is skipped because it is generated`,
    "nothing else may be skipped: a path nobody generates is a SOURCE, its "
      + "change is one the board has to look at, and skipping it is how a board "
      + "stops moving",
  );
}

// THE REPAIR SLICE ADVANCES BY WHAT IT TOOK, and the workflow may not say
// otherwise. `--refresh-from` pins the offset, and pinning it to the run number
// is the shape that stalled the board for 35 hours: a slice is hundreds of rows
// wide and the run number steps by one, so consecutive runs re-fought the same
// rows and a board of 7,659 would have needed 7,659 runs to cross itself once.
// The cursor in `data/board_state.yaml` is the only source, and it is silent
// when it is wrong — the runs all go green.
const pinned = wf.filter((l) => l.includes("--refresh-from"));
check(pinned.length === 0, "the repair slice starts where the last one stopped",
  `the workflow pins the offset (${pinned.map((l) => l.trim()).join(" | ")}) instead of `
    + "letting it come from the stored cursor");

console.log(NL + (bad ? `${bad} failed` : "the board skips only what is generated"));
process.exit(bad ? 1 : 0);
