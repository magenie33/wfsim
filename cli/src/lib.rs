// SPDX-License-Identifier: AGPL-3.0-or-later
//! The pipeline binaries' shared code: the board scorer's phases, and the flag
//! parser `wfsim-board` and `wfsim-intake` both read.

pub mod args;
pub mod board;
