// SPDX-License-Identifier: AGPL-3.0-or-later
//! THE SCORER — turns submitted builds into a board. The run is
//! `wfsim_cli::board`, whose module docs say what it reads and writes.

fn main() {
    wfsim_cli::board::run();
}
