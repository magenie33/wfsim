[package]
name = "wfsim-engine"
description = "wfsim damage-pipeline engine: pure functions, each layer independently testable (see docs/CORE.md section 3)"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

# build.rs parses every data/*.yaml before and after stripping its comments
# and refuses to emit one whose meaning moved, so the guarantee is a BUILD
# failure rather than a test somebody has to run.
[build-dependencies]
serde_norway.workspace = true

[dependencies]
serde.workspace = true
serde_norway.workspace = true
