// SPDX-License-Identifier: AGPL-3.0-or-later
//! BUILD — a loadout resolved into the numbers a fight reads: the panel
//! (`loadout`), the Forma a layout costs (`forma`), the axes a scenario
//! exposes (`scenario`) and what a riven rolls (`rivens`).

pub mod forma;
pub mod loadout;
pub mod rivens;
pub mod scenario;
