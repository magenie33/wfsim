// SPDX-License-Identifier: AGPL-3.0-or-later
//! BOARD — what makes two builds the same row (`builds`) and the rulers every
//! row is measured by (`benchmarks`).

pub mod benchmarks;
pub mod builds;
